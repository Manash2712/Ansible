Introduction: Here we have our initial playbook that was created to deploy a full stack web application on a single server in a monolithic fashion. 
First we would like to run the same playbook on 10 different servers. Check the inventory file for list of servers. 

Task: Update the playbook to run against all 10 servers. Use the group name to target all servers in the group.
