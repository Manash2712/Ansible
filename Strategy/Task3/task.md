Introduction: Let's take it one step further. The serial directive has some advanced use cases. For example you could run the play against 2 servers first, then 3 in the next round and then the last 5 together by providing an array as input to the serial directive. This is really useful for "Rolling Updates". 

Task: Update serial directive to use an array instead of a value. Provide 2,3,5 as values. 
