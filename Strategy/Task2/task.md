Introduction: Currently Ansible runs tasks on all of them at once. We would like Ansible to limit execution on just 2 servers at once. 

Task: Use the serial directive to run the tasks on only 2 servers at a time. 
