Introduction: We are given two lists of dependent packages to install. A playbook has been written with a task to install dependencies. 

Task: Identify the unique packages from the two lists (web_dependencies and sql_dependencies) using jinja2 filter union/intersect and apply it in the given space under with_items. 
